{
  "name": "blocks-mcp-server",
  "version": "0.1.0",
  "description": "MCP server for managing blocks schemas via API",
  "main": "./src/index.js",
  "type": "commonjs",
  "scripts": {
    "start": "node ./src/index.js",
    "debug": "node --inspect-brk ./src/index.js",
    "test": "node ./tests/client.js",
    "test-simple": "node ./tests/client-simple.js",
    "test-fixed": "node ./tests/client-fixed.js",
    "test-debug": "node --inspect-brk=9230 ./tests/client.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^0.5.0"
  },
  "engines": {
    "node": ">=20"
  },
  "keywords": [
    "mcp",
    "model-context-protocol",
    "schema-management",
    "database"
  ],
  "author": "Your Name",
  "license": "MIT",
  "mcp": {
    "tools": [
      {
        "name": "create_schema",
        "description": "Create a new database schema with field definitions"
      },
      {
        "name": "update_schema", 
        "description": "Update an existing database schema"
      }
    ]
  }
}